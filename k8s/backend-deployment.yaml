apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: ghcr.io/jwsid24/fortune-backend:latest
          ports:
            - containerPort: 9000
          env:
            - name: REDIS_DNS
              value: redis
